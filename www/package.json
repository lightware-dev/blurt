{
    "name": "@blurt/www",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "next dev --turbopack",
        "build": "next build",
        "start": "next start",
        "og": "./scripts/generate-og.sh",
        "deploy": "opennextjs-cloudflare build && opennextjs-cloudflare deploy",
        "preview": "opennextjs-cloudflare build && opennextjs-cloudflare preview"
    },
    "dependencies": {
        "@opennextjs/cloudflare": "^1.20.2",
        "next": "^16.2.6",
        "react": "^19.2.8",
        "react-dom": "^19.2.8"
    },
    "devDependencies": {
        "@tailwindcss/postcss": "^4",
        "@types/node": "^20.19.21",
        "@types/react": "^19",
        "@types/react-dom": "^19",
        "tailwindcss": "^4",
        "typescript": "^7",
        "wrangler": "^4.115.0"
    },
    "//overrides": "Transitive advisories that next still pins below their patched versions. Drop each entry once next ships it: postcss >8.5.17 (GHSA-r28c-9q8g-f849 path traversal), sharp >=0.35.0 (libvips CVE-2026-33327/33328/35590). brace-expansion (GHSA-mh99-v99m-4gvg) is deliberately NOT overridden: 5.0.8 is ESM-only and breaks minimatch@8, pinned by glob@9.3.5 inside @node-minify/core, which fails the opennextjs-cloudflare build. It is a build-time globbing DoS reachable only through attacker-controlled glob patterns, and nothing in the build takes patterns from outside this repo.",
    "overrides": {
        "postcss": "^8.5.24",
        "sharp": "^0.35.3"
    }
}
