# Python
.venv/
__pycache__/
*.pyc

# Secrets / local config
.env

# Self-signed TLS (regenerate per machine: scripts/gen_certs.sh)
certs/

# Swift / macOS build artifacts
clients/mac/.build/
clients/mac/Blurt.app/
clients/mac/dist/
.DS_Store

# .NET / Windows build artifacts
clients/windows/bin/
clients/windows/obj/
clients/windows/publish/
clients/windows/dist/

# Windows code-signing material must never be committed.
*.pfx

# Signing material must never be committed — keep it in a password manager.
*.p12
*.p8
*.cer
*.certSigningRequest

# Model / HF caches (if ever placed in-tree)
*.nemo
